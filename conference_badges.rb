# Write your code here.

def badge_maker(name)
  puts "Hello, my name is #{name}"
  
def badge_badge_creator(speaker).each do |speaker|
    speaker.each { "Dr. #{speaker} is an expert in ruby rails." }
  
def assign_rooms(suite).index do |suite|
  suite.index { "#{speaker} is staying in #{suite.index += 1}" }
  
def printer(details)
  puts "#{speaker} assigned to room #{suite.index}"
  
  end
end
  
  speaker_array =["Edsger", "Ada", "Charles", "Alan", "Grace", "Linus", "Matz"]